# Hidden Prize Puzzle

Single-page drag-and-drop image puzzle that reveals a video when completed.

To change the puzzle later, edit `index.html`:

- `COLS` and `ROWS` control puzzle size.
- `VIDEO_URL` controls the revealed video.
- `--outline-color` controls the puzzle guide visibility.
